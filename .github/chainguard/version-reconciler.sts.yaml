# Copyright 2025 Chainguard, Inc.
# SPDX-License-Identifier: Apache-2.0

issuer: https://accounts.google.com

# version-reconciler@prod-enforce-fabc.iam.gserviceaccount.com
# TODO: Update this with the actual unique ID once the service account is created
# To get the unique ID: gcloud iam service-accounts describe version-reconciler@prod-enforce-fabc.iam.gserviceaccount.com --project=prod-enforce-fabc --format="value(uniqueId)"
subject: "REPLACE_WITH_UNIQUE_ID"

permissions:
  contents: read # to fetch releases and tags from repositories
  metadata: read # to read repository metadata

repositories: [] # Act over all of the repos in the org.
